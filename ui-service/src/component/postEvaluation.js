import React, { Fragment } from "react";
import { Button, Form } from "react-bootstrap";

export default function PostEvaluation(props) {

    return (
        <>
            <span>{props.postEvalQuestion}</span>
        </>
    );
}